# IFA v0.6 — benchmark label corrections

**None.**

No case in `evaluation/claims/corpus.ts` or `evaluation/claims/corpus-v05.ts` had
its `expected` relation, `matchLevel`, `claimType` or any other label changed
during v0.6. `git diff 9df590f -- evaluation/claims/` is empty.

Every recall improvement came from the engine (`src/lib/…`), not from relabelling
a case the engine disagreed with. The 10 previously-failing pairs
(`C05 J10 P04 P12 P13 P14 Q06 Q11 Q14 S10`) now pass against their **original**
human labels.
